import React from 'react';
import { _window } from 'douhub-ui-web-basic';
import { PageList } from 'douhub-ui-web-premium';

const ListMainArea = (props: Record<string, any>) => {
    return (
        <PageList {...props}/>
    )
}


export default ListMainArea;